{% extends 'root.html.twig' %}

{% block content %}
    <h2>{{ cuisine.getName }}</h2>
    <p><a href='/cuisines/{{ cuisine.getId }}/edit'>Edit this category</a></p>
    <ul>
        {% for restaurant in restaurants %}
            <li><a href='/restaurants/{{ restaurant.getId }}'>{{ restaurant.getName }}</a></li>
        {% endfor %}
    </ul>
    <p><a href='/'>Back</a></p>
{% endblock %}
